let API_BASE_URL;

// if (process.env.NODE_ENV === "production") {
  API_BASE_URL = "https://app-shmeeter-server-production.up.railway.app";
// } else {
  // API_BASE_URL = "http://localhost:1337";
// }

export { API_BASE_URL };
